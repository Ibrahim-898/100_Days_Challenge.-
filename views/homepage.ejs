<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <script src="https://cdn.tailwindcss.com"></script>
     <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <title>Homepage</title>
</head>
<body class="bg-gradient-to-t from-blue-400  to-white-500 h-screen flex flex-col">
  
  <nav class="bg-indigo-600 flex items-center justify-between">
  <div class="flex justify-center px-4 space-x-6">
    <h3>Logo</h3>
    <form action="" class="flex justify-center">
        <input type="text" placeholder="search" name="search" class="rounded-lg p-1 w-80 text-black">
    </form>
  </div>
  <ul class="flex items-center p-2 space-x-3">
    <li> <a href="'#"> <i class="fa-solid fa-user"></i> </a></li>
    <li> <a class="bg-red-600 rounded-lg px-3 py-2 text-sm mb-3" href="http://logout">Logout</a></li>
  </ul>
  </nav>
    <main class="flex-grow flex items-center justify-center p-6">
      <div class="w-full max-w-xl space-y-6">
        <div>
   <h3 class="text-black text-xl mb-3">Create a new Post.</h3>
    <form method="post" action="/post" class="flex item-start p-2 text-black space-x-3 ">
        <textarea placeholder="What's on Your Mind!!!" class=" w-full resize-none bg-white border-2 rounded-lg border-indigo-600" name="content"></textarea>
        <input type="submit" value="Create Post"  class=" bg-indigo-600 text-xl text-white rounded-lg p-2" >
    </form>
      </div>
      <div class="posts ">
        <h3 class="font-semibold mb-4"> Your Posts :</h3>
        <div class="postcontainer space-y-4 ">
          
           <%  user.posts.forEach(function(post){ %>
            <div class="post  p-3  mb-3 rounded-lg border-2 border-indigo-600 bg-white ">
              <p class="text-blue-900 text-sm font-semibold mb-1"><%=user.name%> </p>
               <p class="text-gray-800 text-sm mb-3"><%= post.content %> </p>
                <small class="mt-2 inline-block"> <%= post.likes.length %> </small>
                <div class="flex justify-between space-x-3 text-sm">
                 
                 <a href="/like/<%=post._id%>" class="text-blue-900 mr-4"> 
                  <%= post.likes.indexOf(user._id)===-1 ? "Like" : "Unlike" %>
                 </a>
                 <a href="/edit/<%=post._id%>" class="text-black  px-2 py-1 rounded">Edit</a>
                 </div>
                  </div>
                   <% }); %>
        </div>
        </div>
        
    </div>
    </main>
</body>
</html> 